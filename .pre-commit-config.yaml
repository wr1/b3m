repos:
  - repo: local
    hooks:
      - id: enforce-git-sources
        name: Enforce Git Sources
        entry: python scripts/enforce_git_sources.py --git
        language: system
        pass_filenames: false
        files: pyproject.toml